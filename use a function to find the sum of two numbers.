#include <stdio.h>
 
int sumTwo(int a, int b)
{
  	return a + b;
}
 
int main()
{
  	int x, y;
  	printf("25331A05F0\n");
  	printf("Enter two numbers: ");
  	scanf("%d %d", &x, &y);
  	printf("Sum = %d\n", sumTwo(x, y));
  	return 0;
}
